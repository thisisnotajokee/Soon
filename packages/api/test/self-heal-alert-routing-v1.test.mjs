import test from 'node:test';

test.todo(
  'self-heal alert routing v1: enforces purchase->telegram and technical->discord after auto-remediation',
);

test.todo(
  'self-heal alert routing v1: records dead-letter retry_budget_exhausted and exposes exhausted/backoff metrics',
);
