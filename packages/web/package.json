{
  "name": "@soon/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173 --strictPort",
    "build": "vite build",
    "build:runtime": "tsc --noEmit false --declaration false --sourceMap false",
    "preview": "vite preview --host 0.0.0.0 --port 4173 --strictPort",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "typescript": "^5.7.3",
    "vite": "^8.0.9",
    "vitest": "^4.1.5"
  }
}
